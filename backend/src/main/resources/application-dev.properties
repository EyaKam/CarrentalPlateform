# Development Environment Configuration
# Usage: mvn spring-boot:run -Dspring-boot.run.arguments="--spring.profiles.active=dev"
# Or: java -jar app.jar --spring.profiles.active=dev

# Application
spring.application.name=backend
server.port=8080

# MongoDB Atlas - Development
spring.data.mongodb.uri=mongodb+srv://eyakam820_db_user:admin123@carrentalcluster.dokysl6.mongodb.net/carrental?appName=carrentalcluster
spring.data.mongodb.database=carrental

# CORS Configuration - Development (Relaxed for easier testing)
# Multiple localhost origins for different development ports
cors.allowed-origins=http://localhost:3000,http://localhost:4200,http://localhost:5000,http://localhost:5173
cors.allow-credentials=true
cors.max-age=3600

# Logging - Development
logging.level.root=INFO
logging.level.com.carrental=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.mongodb.driver=DEBUG
